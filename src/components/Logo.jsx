import { LOGO } from "../utils/constants";

const Logo = () => {
  return (
    <div className="logo">
      <img
        className="logo"
        src={LOGO}
      ></img>
      <h1 className="logo-head">Zesty </h1>
    </div>
  );
};
export default Logo;
